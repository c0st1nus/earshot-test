[package]
name = "earshot_vad_demo"
version = "0.1.0"
edition = "2021"

[dependencies]
# VAD library being tested: https://github.com/pykeio/earshot
earshot = "1"
# Cross-platform audio capture (mic input)
cpal = "0.15"
